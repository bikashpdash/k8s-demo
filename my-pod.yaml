apiVersion: v1
kind: Pod
metadata:
  name: myapp-pod
  labels:
    name: backend
spec:
  containers:
  - name: ngnix-container
    image: ngnix
    resources:
      limits:
        memory: "128Mi"
        cpu: "100m"